= form_for @snippet do |f|
  -if @snippet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@snippet.errors.count, "error")} prohibited this snippet from being saved:"
      %ul
        - @snippet.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :author
    .input= f.text_field :author, class: 'xlarge'
  .field
    = f.label :title
    .input= f.text_field :title, class: 'xlarge'
  .field
    = f.label :body
    .input= f.text_area :body, class: 'xxlarge span12', rows: 30
  .field
    = f.label :filetype
    .input= f.select :filetype, CodeRay::FileType::TypeFromExt.values.uniq.sort.map(&:to_s), class: 'xlarge'
  .actions
    = button_tag 'Clear', type: 'reset', class: 'btn'
    = f.submit 'Save', class: 'btn primary'

